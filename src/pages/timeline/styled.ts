import styled from "styled-components";

export const Main = styled.main`
    display: flex;
    flex-direction: column;
    gap: 1rem;
   

    section {
        display: flex;
        gap: 1rem;
    }

    .contentBox {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
`